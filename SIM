#!/usr/bin/python
import sys
import os
import math
from random import expovariate # exponenial(lamb)
from random import normalvariate #(mu, sigma)
from random import uniform
from statistics import mean
from statistics import median
import matplotlib.pyplot as plt
import numpy as np

#importing of the class files
from classes import safety_signals
from classes import peds 
from classes import autos 


n = sys.argv[1]
autoAriivalSpeed = sys.argv[2] #Uniform(0,1)
pedArrivalSpeed = sys.argv[3] #Uniform(0,1)
crosswalkButtonPress = sys.argv[4] #Uniform(0,1)



class crosswalksignal(ENUM):
    NO_WALK = 0
    WALK = 1

class trafficlight(ENUM):
    RED = 0
    YELLOW = 1
    GREEN = 2      










event_list =  []

#generate n aumobiles

 while events_list != []:
    e = events_list.pop(0)

    #different types of events